@model List<Domain.Models.Apartment>

@{
    ViewData["Title"] = "Apartments";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    @for (int i = 0; i < Model.Count; i++)
    {
        if (i % 3 == 0)
        {
            if (i != 0)
            {
                @:</div>
            }
            @:<div class="row">
        }

        var item = Model[i];
        <div class="col-md-4 mb-4">
            <div class="card" style="width: 18rem;">
                <a asp-action="Details" asp-controller="Apartments" asp-route-id="@item.Id">
                    <img class="card-img-top" src="@item.ImageUrl" alt="@item.Name">
                </a>
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>
                    <p>@item.Location</p>
                </div>
            </div>
        </div>
    }

    @if (Model.Count % 3 != 0)
    {
        @:</div>
    }
</div>

<style>
    .row {
        display: flex;
        margin-top: 20px;
    }
    img {
        width: 100%;
        height: auto;
    }
    .card {
    box-shadow: 0 4px 10px rgba(160, 66, 178, 0.5); 
    border-radius: 6px;
    border: rgba(170, 7, 178, 0.5); 
    overflow: hidden; 
    transition: transform 0.3s ease-in-out;
}

.card:hover {
    transform: scale(1.05);
}

.card img {
    border-top-left-radius: 6px;
    border-top-right-radius: 6px;
    height: 200px; 
    object-fit: cover; 
}

</style>